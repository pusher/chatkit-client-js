import {
  BaseClient,
} from 'pusher-platform';

import ChatManager from './chat_manager';
import TokenProvider from './token_provider';

export {
  ChatManager,
  TokenProvider,
  BaseClient,
};

export default {
  ChatManager,
  TokenProvider,
  BaseClient,
};
